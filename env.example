# ===========================================
# CONFIGURAÇÕES LOCAIS (Docker Compose)
# ===========================================

# Configurações do Banco de Dados PostgreSQL
# Baseado no docker-compose.yml
DATABASE_URL="postgresql://root:3636@localhost:5433/grafica-api"

# Configurações do Redis
# Baseado no docker-compose.yml
REDIS_URL="redis://localhost:6379"

# ===========================================
# CONFIGURAÇÕES PARA RAILWAY
# ===========================================

# No Railway, você deve configurar estas variáveis no painel:
# DATABASE_URL - URL do PostgreSQL fornecida pelo Railway
# REDIS_URL - URL do Redis fornecida pelo Railway (se usar Redis addon)

# Configurações JWT
JWT_SECRET="seu-jwt-secret-super-seguro-aqui"
JWT_EXPIRES_IN="30d"

# Configurações da Aplicação
PORT=3000
NODE_ENV=development

# Configurações CORS (opcional)
PAINEL_URL="http://localhost:3001"

# ===========================================
# INSTRUÇÕES PARA RAILWAY
# ===========================================
# 1. No painel do Railway, vá em "Variables"
# 2. Adicione as seguintes variáveis:
#    - DATABASE_URL (fornecida automaticamente se usar PostgreSQL addon)
#    - REDIS_URL (fornecida automaticamente se usar Redis addon)
#    - JWT_SECRET (defina uma chave segura)
#    - JWT_EXPIRES_IN (opcional, padrão: 30d)
#    - NODE_ENV=production
#    - PAINEL_URL (URL do seu frontend)
# 3. O PORT é definido automaticamente pelo Railway
